class Drop {
  double x;
  double y;
  double length;
  double speed;
  double opacity;

  Drop({required this.x, required this.y, required this.length, required this.speed, required this.opacity});
}
